
#include "functions.h"
#include <string.h>

bool is_palindromo(char texto[]){
	tam = strlen(texto) 
	if( tam == 1){
		return 1;
	}
	else{
		if( texto[0] != texto[tam-1])
			return 0;
	}
		for( int i )
		is_palindromo()

}
