#include <stdio.h>
#include "functions.h"

int main() {

  char palavra[20];
  
  // ENTRADA DE DADOS
  scanf("%c", &palavra)
	

  // PROCESSAMENTO - EXECUTANDO A FUNCAO
  vogais = numero_vogais(palavra);

  // SAIDA - IMPRIMINDO O RESULTADO
  printf("SOMA = %d\n", s);

  return(0);
}